Use `<verdict>` as the XML tag for machine-readable verdicts.

The tag `<summary>` wraps the summary block.

This line has `<unclosed>` in inline code — should not trigger V-04.

Plain text with no raw XML.
