## Overview

This is a legacy v2-format architecture document. It uses plain English headings
that are not in the allowed set for the `architecture` artifact type.

## Architecture

The system adds a helper function to a scratch script. No external dependencies.
One file changed. Backward compatible.

## Risks

Minimal risk. The scratch file may not exist; create it if needed.

## Next Steps

Proceed to implementation. No planning artifacts needed.
